{
  "name": "bharat-jewellers-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/postbuild-sitemap-alias.mjs",
    "preview": "astro preview --host",
    "check": "astro check",
    "migrate-blog": "node scripts/migrate-wp-blog.mjs",
    "fix-blog-links": "node scripts/fix-blog-internal-links.mjs",
    "deploy": "npm run build"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "fast-xml-parser": "^5.5.11",
    "turndown": "^7.2.4"
  }
}
